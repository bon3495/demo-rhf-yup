import { styled } from '@mui/system';

export const FormContainer = styled('form')({
  maxWidth: 760,
  width: '100%',
  height: '100%',
  padding: '80px 40px',
  border: '1px solid #ccc',
  borderRadius: 5,
});
